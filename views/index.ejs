<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lyrics</title>
    <link rel="stylesheet" href="styles/styles.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    
</head>
<body>
    <% if (locals.content) { %> 
    <a href="<%= content %>">Go to lyrics page</a>
    <% } else { %>
     
        
        <form action="/submit" method="post">
            <label for="userPrompt">Search Lyrics</label>
            <input type="text" name="userPrompt" id="userPrompt" placeholder="Taylor Swift Antihero" required autocomplete="off">
            <input type="submit" value="Search">
        </form>
        <% } %>


        <% if (locals.err) { %>
            <p>The song you are looking for could not be found. </p>
            <% } %>
           
</body>

<script>
    const form = document.querySelector('form');
  const submitButton = form.querySelector('input[type="submit"]');

  form.addEventListener('submit', function() {
    submitButton.disabled = true;
  
    submitButton.value = "Searching...";
   
  });
    </script>
</html>